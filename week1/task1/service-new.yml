apiVersion: v1
kind: Service
metadata:
  name: web-server-service-new
  namespace: task1
spec:
  selector:
    app: nginx-proxy
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: NodePort
